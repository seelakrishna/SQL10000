CREATE TABLE Company (
    company_id INT PRIMARY KEY,
    company_name VARCHAR(100),
    company_location VARCHAR(100)
);

CREATE TABLE Department (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(100),
    dept_location VARCHAR(100),
    company_id INT,
    FOREIGN KEY (company_id) REFERENCES Company(company_id)
);

CREATE TABLE Employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(100),
    salary DECIMAL(10,2),
    dept_id INT,
    FOREIGN KEY (dept_id) REFERENCES Department(dept_id)
);

INSERT INTO Company VALUES
(1, 'Infosys', 'Hyderabad'),
(2, 'TCS', 'Bangalore');

INSERT INTO Department VALUES
(101, 'HR', 'Hyderabad', 1),
(102, 'IT', 'Bangalore', 1),
(103, 'Finance', 'Chennai', 2);

INSERT INTO Employees VALUES
(1, 'Rahul', 60000, 101),
(2, 'Sneha', 45000, 102),
(3, 'Arjun', 70000, 103),
(4, 'Priya', 52000, 101);

SELECT e.emp_name, d.dept_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id;

SELECT e.emp_name, e.salary, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;

SELECT e.*
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id;

SELECT e.emp_name, e.salary, d.dept_location
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id;

SELECT e.*
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
WHERE d.dept_name = 'HR';

SELECT e.emp_name, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;

SELECT e.emp_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;

SELECT e.emp_name, d.dept_name, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;

SELECT e.emp_name, d.dept_name, e.salary
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
WHERE e.salary > 50000;

SELECT e.emp_name, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id
WHERE c.company_name = 'Infosys';

CREATE TABLE Theatre (
    theatre_id INT PRIMARY KEY,
    theatre_name VARCHAR(100),
    location VARCHAR(100)
);

CREATE TABLE Movie (
    movie_id INT PRIMARY KEY,
    movie_name VARCHAR(100),
    show_time VARCHAR(50),
    theatre_id INT,
    FOREIGN KEY (theatre_id) REFERENCES Theatre(theatre_id)
);

CREATE TABLE Booking (
    booking_id INT PRIMARY KEY,
    customer_name VARCHAR(100),
    movie_id INT,
    booking_date DATE,
    FOREIGN KEY (movie_id) REFERENCES Movie(movie_id)
);


INSERT INTO Theatre VALUES
(1, 'PVR', 'Hyderabad'),
(2, 'INOX', 'Bangalore');

INSERT INTO Movie VALUES
(101, 'Pushpa', '6 PM', 1),
(102, 'RRR', '9 PM', 2);

INSERT INTO Booking VALUES
(1, 'Rahul', 101, '2026-05-01'),
(2, 'Sneha', 102, '2026-05-02'),
(3, 'Arjun', 101, '2026-05-03');



SELECT b.booking_id, b.customer_name, m.movie_name
FROM Booking b
INNER JOIN Movie m
ON b.movie_id = m.movie_id;


SELECT m.movie_name, t.theatre_name
FROM Booking b
INNER JOIN Movie m
ON b.movie_id = m.movie_id
INNER JOIN Theatre t
ON m.theatre_id = t.theatre_id;

SELECT b.customer_name, m.movie_name
FROM Booking b
INNER JOIN Movie m
ON b.movie_id = m.movie_id;

SELECT DISTINCT m.movie_name
FROM Movie m
INNER JOIN Booking b
ON m.movie_id = b.movie_id;

SELECT b.booking_id, b.customer_name, t.location
FROM Booking b
INNER JOIN Movie m
ON b.movie_id = m.movie_id
INNER JOIN Theatre t
ON m.theatre_id = t.theatre_id;

SELECT m.movie_name, m.show_time, t.theatre_name
FROM Movie m
INNER JOIN Theatre t
ON m.theatre_id = t.theatre_id;

SELECT b.*
FROM Booking b
INNER JOIN Movie m
ON b.movie_id = m.movie_id
WHERE m.movie_name = 'Pushpa';

SELECT DISTINCT t.theatre_name
FROM Theatre t
INNER JOIN Movie m
ON t.theatre_id = m.theatre_id
INNER JOIN Booking b
ON m.movie_id = b.movie_id;

SELECT m.movie_name, COUNT(b.booking_id) AS total_bookings
FROM Movie m
INNER JOIN Booking b
ON m.movie_id = b.movie_id
GROUP BY m.movie_name;

SELECT b.booking_id, b.customer_name, m.movie_name, t.theatre_name
FROM Booking b
INNER JOIN Movie m
ON b.movie_id = m.movie_id
INNER JOIN Theatre t
ON m.theatre_id = t.theatre_id;

CREATE TABLE Students (
    student_id INT PRIMARY KEY,
    student_name VARCHAR(100),
    branch VARCHAR(50)
);

CREATE TABLE Placement (
    placement_id INT PRIMARY KEY,
    student_id INT,
    company_name VARCHAR(100),
    package DECIMAL(10,2),
    location VARCHAR(100),
    FOREIGN KEY (student_id) REFERENCES Students(student_id)
);


INSERT INTO Students VALUES
(1, 'Rahul', 'CSE'),
(2, 'Sneha', 'ECE'),
(3, 'Arjun', 'IT');

INSERT INTO Placement VALUES
(101, 1, 'Infosys', 600000, 'Hyderabad'),
(102, 2, 'TCS', 550000, 'Bangalore');

SELECT s.student_name
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;


SELECT s.student_name, p.company_name
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;

SELECT s.student_name, p.package
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;


SELECT s.student_name
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id
WHERE p.company_name = 'Infosys';

SELECT s.student_name, s.branch, p.company_name
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;

SELECT s.student_name, s.branch, p.company_name
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;

SELECT s.student_name
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;

SELECT s.student_name, p.package
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id
WHERE p.package > 500000;


SELECT s.student_name, p.location
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;

SELECT s.*
FROM Students s
INNER JOIN Placement p
ON s.student_id = p.student_id;


SELECT e.emp_name, d.dept_name, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;

FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;


SELECT e.emp_name, d.dept_name, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id;

SELECT e.emp_name, d.dept_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id;

SELECT e.emp_name, d.dept_name, c.company_name
FROM Employees e
INNER JOIN Department d
ON e.dept_id = d.dept_id
INNER JOIN Company c
ON d.company_id = c.company_id
WHERE e.salary > 50000;
